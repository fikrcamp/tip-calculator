// Good Luck! You got this 💪🏾
// Write your code here.
let bill = 275
let tip1 = bill * 0.15
let tip2 = bill * 0.20
let total_v1 = bill + tip1
let total_v2 = bill + tip2
if(bill >= 0 && bill <= 300){
    console.log(`th bill was ${bill}$ the tip was ${tip1}$ and the total value is ${total_v1}$`)
}else{
    console.log(`the bill was ${bill}$ the tip was ${tip2}$ and the total value is ${total_v2}$`)
}
